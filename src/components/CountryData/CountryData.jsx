/* eslint-disable no-unused-vars */

const CountryData = ({cc,rr,tt}) => {
    return (
        <div>
            <p><small>Country Data :{cc.name.common}</small></p>
        </div>
    );
};

export default CountryData;